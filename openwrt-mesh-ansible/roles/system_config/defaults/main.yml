---
# Default variables for system_config role
# These are fallbacks - prefer setting in group_vars/all.yml

# System configuration defaults are sourced from group_vars/all.yml
# This file provides documentation of required variables:
#
# Required variables:
#   - root_password: Root password for console access
#   - hostname_prefix: Hostname prefix (e.g., 'mesh-node')
#   - node_id: Node identifier (e.g., 1, 2, 3)
#   - timezone: Timezone string (e.g., 'AEST-10')
#   - zonename: Zone name (e.g., 'Australia/Adelaide')
