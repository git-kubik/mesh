# Docker Compose Environment Variables
# IMPORTANT: Copy this file to .env and update with your actual values
# .env is excluded from Git via .gitignore
# ALL variables below are REQUIRED - no defaults are provided

# PostgreSQL Database
# Strong password for PostgreSQL database (min 16 chars recommended)
POSTGRES_PASSWORD=

# Semaphore Web Interface
# Port to expose Semaphore on localhost
SEMAPHORE_PORT=3000

# Semaphore admin username (for login)
SEMAPHORE_ADMIN=admin

# Semaphore admin password (REQUIRED - min 12 chars recommended)
SEMAPHORE_ADMIN_PASSWORD=

# Semaphore admin display name
SEMAPHORE_ADMIN_NAME=Admin

# Semaphore admin email
SEMAPHORE_ADMIN_EMAIL=admin@localhost

# Semaphore Access Key Encryption
# Generate with: head -c32 /dev/urandom | base64
# Leave empty on first run, Semaphore will generate if needed
SEMAPHORE_ACCESS_KEY_ENCRYPTION=

# Mesh Network Variables (for Ansible playbooks)
# These are passed through to the Ansible container

# 2.4GHz mesh network password (WPA2-PSK, min 8 chars)
MESH_PASSWORD=

# 5GHz client network password (WPA2-PSK, min 8 chars)
CLIENT_PASSWORD=

# Guest network password (WPA2-PSK, min 8 chars)
GUEST_PASSWORD=

# Mesh network ID (SSID for mesh network)
MESH_ID=ha-mesh-net

# Client network SSID (5GHz AP)
CLIENT_SSID=HA-Network-5G
